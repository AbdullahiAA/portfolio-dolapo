import "./App.css";
import Home from "./pages/Home";

function App() {
   // function hideOpenedMenu() {
   //    const openedMenu = document.querySelector("nav.active");

   //    if (openedMenu) {
   //       openedMenu.classList.remove("active");
   //    }
   // }

   return (
      <div className="app">
         {/* HomePage */}
         <Home />

         {/* Ongoing Projects */}

         {/* Recent Works */}
      </div>
   );
}

export default App;
